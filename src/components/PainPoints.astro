---
const painPoints = [
  {
    icon: '🐌',
    title: 'Throughput Evidence',
    subtitle: 'Python: Global Interpreter Lock',
    description: 'Published benchmark notes list typical Python recursive splitters around 50-100 MiB/s for similar workloads.',
    solution: 'Wesichain recursive splitter measures 201-221 MiB/s in current Criterion artifacts.',
    stat: '2-4x',
    statLabel: 'splitter throughput range'
  },
  {
    icon: '💾',
    title: 'Reproducible Artifacts',
    subtitle: 'Performance claims need provenance',
    description: 'Marketing numbers drift fast when they are not tied to versioned benchmark outputs.',
    solution: 'Wesichain keeps benchmark snapshots under docs/benchmarks/data with run metadata and hardware context.',
    stat: 'JSON',
    statLabel: 'snapshot artifacts'
  },
  {
    icon: '📦',
    title: 'Methodology Transparency',
    subtitle: 'Re-run locally anytime',
    description: 'Benchmark trust comes from reproducibility, not one-off charts.',
    solution: 'The docs include runnable commands and dataset references so teams can validate on their own hardware.',
    stat: '1 cmd',
    statLabel: 'to reproduce splitter benchmark'
  }
];
---

<section class="py-24 bg-neutral-900/30 border-y border-neutral-800">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
        Why Rust for LLM Agents?
      </h2>
      <p class="mt-4 text-lg text-neutral-400">
        Keep performance messaging anchored to benchmark artifacts
      </p>
    </div>

    <div class="grid gap-8 md:grid-cols-3">
      {painPoints.map((point) => (
        <div class="rounded-2xl border border-neutral-800 bg-neutral-900 p-8">
          {/* Icon */}
          <div class="text-4xl mb-4">{point.icon}</div>

          {/* Title */}
          <h3 class="text-xl font-semibold text-white mb-2">
            {point.title}
          </h3>
          <p class="text-sm text-neutral-500 mb-4">{point.subtitle}</p>

          {/* Pain */}
          <div class="mb-4">
            <p class="text-sm text-red-400 mb-1 font-medium">The Problem</p>
            <p class="text-neutral-400 text-sm">{point.description}</p>
          </div>

          {/* Solution */}
          <div class="mb-6">
            <p class="text-sm text-green-400 mb-1 font-medium">The Solution</p>
            <p class="text-neutral-300 text-sm">{point.solution}</p>
          </div>

          {/* Stat */}
          <div class="pt-4 border-t border-neutral-800">
            <div class="flex items-baseline gap-2">
              <span class="text-3xl font-bold text-rust-500">{point.stat}</span>
              <span class="text-sm text-neutral-400">{point.statLabel}</span>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
