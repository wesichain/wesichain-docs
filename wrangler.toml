# Cloudflare Pages — build configuration
# Deploy: wrangler pages deploy dist --project-name=wesichain
# Or connect the GitHub repo at: dash.cloudflare.com → Pages → Create application

name = "wesichain"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

[env.production.vars]
NODE_VERSION = "20"
